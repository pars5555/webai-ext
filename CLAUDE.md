# This repository has moved

The WebAI extension now lives **inside the server repository**, under `extension/`:

| | |
|---|---|
| Repository | `pars5555/webai-server` |
| Local path | `d:\xampp\htdocs\webai-server` |
| Extension path | `d:\xampp\htdocs\webai-server\extension\` |

One app, one repository. The full history of this repository was carried across with
`git subtree`, so every commit here also exists there — nothing was flattened or lost.

**Do not commit here.** This repository is archived and read-only on GitHub. Work on the
extension in `d:\xampp\htdocs\webai-server\extension\`, and read the project guide at
`d:\xampp\htdocs\webai-server\CLAUDE.md`, which now covers both halves of the product.

The local folder `d:\xampp\htdocs\webai-ext` is a redundant clone and is safe to delete.
